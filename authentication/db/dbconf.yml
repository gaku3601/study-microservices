development:
  driver: postgres
  open: user=postgres dbname=auth_db host=auth-db port=5432 sslmode=disable
production:
  driver: postgres
  open: user=postgres dbname=auth_db host=auth-db port=5432 sslmode=disable
